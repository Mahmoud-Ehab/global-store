import { ExpressServer as Server } from "@sfawd/express";

import { 
  UserEndpoints,
  PublicationEndpoints,
  ReviewEndpoints 
} from "./Endpoints";

export {
  Server,
  UserEndpoints,
  PublicationEndpoints,
  ReviewEndpoints
}